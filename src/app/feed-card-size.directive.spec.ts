import { FeedCardSizeDirective } from './feed-card-size.directive';

describe('FeedCardSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new FeedCardSizeDirective();
    expect(directive).toBeTruthy();
  });
});
